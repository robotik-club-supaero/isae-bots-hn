# Copy to /etc/systemd/system/masterNode.service

[Unit]
Description=MasterNode Service
After=docker.service
Requires=docker.service

[Service]
Type=simple
Restart=always
StandardInput=null

User=pi
Group=pi
UMask=007

ExecStart=/home/pi/isae-bots-hn/scripts/runMasterNode.sh

[Install]
WantedBy=multi-user.target
